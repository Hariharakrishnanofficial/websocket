# Copy this file to `.env.production` after the first AppSail deploy
# and replace the URL with the public AppSail URL printed by `catalyst deploy`.
#
# Catalyst terminates TLS at the edge, so always use wss:// in production.

VITE_WS_URL=wss://appsail-XXXXXXXXXX.development.catalystserverless.com/ws
