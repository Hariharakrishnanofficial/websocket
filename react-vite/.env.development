# Local development -> talk to the AppSail relay via the IDE tunnel.
# Process map (verified):
#   port 3000 -> Catalyst-proxied AppSail (public) -> node index.js
#   port 4600 -> raw node index.js (bypasses proxy, dev-only)
# Use the proxied URL so behaviour matches production.
VITE_WS_URL=wss://krishnanhari-b8hfpk0w-4600.zcodeusers.in/ws
