{
  "name": "robot-ws-server",
  "version": "1.0.0",
  "description": "Low-latency WebSocket relay between browser controller and ESP32 robot.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "ws": "^8.16.0"
  },
  "engines": {
    "node": ">=18"
  }
}
