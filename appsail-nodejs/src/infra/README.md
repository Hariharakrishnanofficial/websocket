# `infra/` — External Integrations

Adapters that touch the outside world. Mockable in tests.

- `catalyst/`  — Catalyst Cache / NoSQL clients
- `metrics/`   — counters & histograms (Prometheus-style export)
- `persistence/` — optional state snapshot writers
